pcl_detector_node:
  ros__parameters:
    leaf_size: 0.1
    detector: "dbscan" # one of "dbscan", "euclidean"
    
    dbscan:
      epsilon: 0.5
      min_points: 10

    euclidean:
      cluster_tolerance: 1.0
      min_points: 10